# 🌙 Current Session — Yappy RAM
*Session memory with 500-line limit. Resets per session, keeps recap for continuity.*

## Session Memory Limit
- **Maximum**: 500 lines
- **Reset Behavior**: RAM-style reset — preserve Session Recap only, clear working details
- **On reset**: Rebuild from `main/session-format.md` template
- **Format Reference**: `main/session-format.md`

---

## 📋 Session Recap (Continuity — survives reset)

**Last session**: Fri Aug 7, 2026 evening (~9:10 PM → 9:41 PM MYT) · PT mode. Hakim asked for a **full ONDW flow redesign** (customer/vendor/rider/admin) using the old Figma-CLI workflow, with a friend's app **BajetBro** (github.com/Tuanwaf/BajetBro) as creative style reference — cloned to `holeeMonth/BajetBro`. Session disconnected right after the plan landed; Hakim reconnected confused about what happened (initially thought it was the PHP-slowness thread) — recovered from the raw session transcript since MemoryCore hadn't been updated yet, now logged properly below. **Team output**: Sora traced BajetBro's real style (soft-brutalist hybrid — thick borders, hard zero-blur offset shadows, mono-font numbers, flat colors, zero gradient/glass) + 5 ways to translate it into ONDW's warm identity; Hana mapped every real screen across all 4 roles straight from the codebase (file + route each); Nadia cross-checked business rules and caught a real contradiction (see reminder below). Synthesized into a 47-frame plan (Customer → Rider → Vendor → Admin → Shared) in a design brief artifact: https://claude.ai/code/artifact/9a2c760a-20e6-4936-b6f7-98ccd2687c63. **Figma Desktop froze** on Hakim's end (looked like a login/2FA prompt) — Mira (who builds the actual frames) never started, still standing by. Hakim said he was heading out, "just plan for now" — session ended there, awaiting his return to either resolve the Figma login or review the brief first.

**Before that**: Wed Aug 5, 2026 evening (~8:44 PM → 8:55 PM MYT, brief) · PT mode requested. Yappy initially answered from the stale Claude-memory snapshot (`ondw-status.md`, last touched Aug 1) instead of reading this file first — same mistake the `memory-architecture` rule already warned about (recurred from the Jun 5 incident). Hakim caught it ("the record seems a bit off") and asked for a refresh; Yappy re-checked MemoryCore, found `current-session.md` had just been updated minutes earlier (20:47) with the parked prod-slowness investigation, and corrected the status. **Fix applied this session**: retired the stale `ondw-status.md` Claude-memory file (deleted + delinked from `MEMORY.md`) so there's no second, staler copy of ONDW status to accidentally read first — MemoryCore's `current-session.md` is now the only place ONDW status lives. Hakim then asked to switch into Plan + Teaching mode combined, but hasn't named a topic yet — Yappy asked which backlog item (or new topic) to focus on; awaiting his answer when this was saved.

**Same day, CLI session (Wed Aug 5, ~8:42 AM → ~8:56 PM MYT, full day)**: FT morning — JPNIN `sismap:sync` dead-scheduler bug found + fixed live on prod with Hakim (details in FT/eFokus reminders below); also applied `->timezone('Asia/Kuala_Lumpur')` to the schedule line locally (was firing at 02:00 UTC = 10 AM MYT — needs Hakim's commit + server pull). PT afternoon — ultra code-review ran (2 confirmed findings on throttle commit `03846f7`, not on main), then full-team prod-slowness investigation (verdict logged below, PARKED by Hakim same night). Evening — Tailscale/Termius phone access confirmed working, homelabbing interest logged. Session ended warmly ~8:56 PM, Hakim heading to Yappy desktop.

**Previous session**: Sat Aug 1, 2026 evening (~9:30 PM → 11:16 PM MYT) · PT ONDW — vendor business-hours cron bug found, fixed, and proven live on prod, plus a personal Tailscale/Termius SSH-key setup on the side. Previous marathon recap (Jul 27–28: Split Dock, Unofficial Vendor + Rider Credit, R2/DNS migration, rider punch list) compacted into `projects/coding-projects/active/ONDEWEI-Laravel.md` — see there for that history.

**Where we left off**:
- 🌐 **Tailscale + Termius** — walked Hakim through remote SSH access to his own Mac (`hakims-macbook-pro`, tailnet `tail931966.ts.net`, iPhone already joined) using key-based auth instead of Tailscale-SSH's identity auth (turned Tailscale SSH back off, enabled macOS Remote Login, generated `~/.ssh/termius_iphone` ED25519 keypair locally, public half added to `authorized_keys`, private half meant for AirDrop → Termius import). Not confirmed working yet — last blocker discussed was a DNS resolution error ("unknown node or service"), advised trying the raw Tailscale IP `100.117.129.35` and confirming the Tailscale app is actually connected on iPhone. Status unconfirmed at session end.
- 🗑️ **Test vendor deletion** — Hana traced full FK graph for `vendor_profiles` (real table name; no literal `vendors` table). Key finding: `orders.vendor_id` has no `onDelete` → defaults to RESTRICT (blocks hard delete if any order history exists); `order_items.item_id` also RESTRICT (blocks cascade through menu_items); `disbursements.recipient_id` has **no FK at all** (silent orphan risk on hard delete). No soft-deletes on vendor_profiles. Recommended flipping `is_active=false` instead of hard-deleting — Hakim agreed, said it's local-only so he'll do it himself via tinker.
- ✅ **Real bug found, fixed, and verified live**: vendor opening/closing-hours auto-toggle wasn't working. Root cause: prod's only cron entries were two `queue:work` restarts (background jobs) — **no `schedule:run` entry existed**, so Laravel's task scheduler (which drives `vendors:apply-schedule`, disbursements, refund retries, PERKESO resubmits, pending-payment expiry — everything in `Kernel::schedule()`) had never actually been ticking on prod, despite all of it being correctly coded and registered (built by Aiman, commit `cef65700`, Jul 5). Fix: added `* * * * * php artisan schedule:run >> /dev/null 2>&1` to prod cron (Hakim's explicit choice: prod only, not preprod). **Verified genuinely firing** via hPanel cron output log at 23:00:02–03 showing Laravel's own `Running [...] ... DONE` scheduler summary actually executing `payments:expire-pending`, `refunds:process`, `vendors:apply-schedule`, `perkeso-retry-sweep`.
- ✅ Also throttled `vendors:apply-schedule` from `everyFiveMinutes()` → `everyFifteenMinutes()` (Hakim's load-reduction request; talked him out of hourly — too much staleness risk for a live ordering app, and 15→30 min barely saves anything since the command itself is cheap). Committed + pushed: `03846f7` on `feature/push-notification`. **Not yet live on prod** — only takes effect once that branch merges to `main` and prod redeploys; prod's `schedule:list` still showed `*/5` this session.
- 🔧 Corrected an assumption mid-session: initially flagged the logout handler + manual online/offline toggle forcing `is_online` regardless of `auto_schedule_enabled` as a bug — Hakim clarified this is intentional (vendor manually closing for an urgent matter should win). Saved as a standing lesson in Claude memory (`dont-assume-bug-verify-intent-first.md`) — don't frame "X overrides Y" findings as bugs without checking intent first.
- ⬜ **Next up, not started**: cron failure alerting. Add `->emailOutputOnFailure()` to the financial scheduled commands (`refunds:process`, `perkeso-retry-sweep`, `disbursements:process-weekly`) in `Kernel.php`. Blocked on SMTP — local `.env` has `MAIL_MAILER=log` (no real send), prod unverified. Hakim confirmed no Hostinger mailbox (would cost extra) — going with **free Gmail SMTP** via `ondeweiofficial@gmail.com` + a Google App Password instead.

**Miyamura's state**: Wrapped up around 11:15 PM after a solid, focused evening session — real bug shipped and proven, not just theorized. Sent to bed warmly, Isyak already covered earlier in the session, Hori mentioned at close.

---

## 🔴 Active Reminders

### FT Mode — mpaj-icomm
- 🟢 **[UPDATE Aug 10 ~5:33 PM] CONNECTIVITY CONFIRMED FIXED end-to-end — pipe is fully open.** Hakim reloaded nginx on the WAF (`mpajicommwaf`) after uncommenting `proxy_pass`, then curled the real OneAPI path FROM the app server (`mpajicommappp1`, part of the `load_balance` upstream) targeting prod: `curl -v -X POST https://icomm2.mpaj.gov.my/celcom/omn/oneapi/smsmessaging/v1/outbound/tel%3AMPAJ/requests` → **`HTTP/1.1 401 Unauthorized`** (no creds sent on purpose), `Server: nginx/1.18.0`, clean TLS handshake, fast response. 401 = Celcom's real gateway answered and rejected for missing auth — not a 404 (nginx routing broken) or 504 (network/proxy dead). Confirms full chain: app server → WAF `/celcom/` proxy → `ems.celcom.com.my:2688` → response bounced back correctly. Only WAF needed the nginx reload (owns the `/celcom/` config); app server doesn't touch nginx. **Year-old firewall/connectivity issue is resolved — this was purely the commented-out proxy_pass, the network ticket theory was a dead end.** **Next (final step)**: real TAC test — trigger an actual password-reset flow in the app, tail `laravel.log` for `Helper.php:1036`'s `"SMS API response"` entry, confirm `status: 201` from Celcom (201 = accepted, not necessarily delivered — Sora's OneAPI research notes 201≠delivery-confirmed), and confirm the SMS physically lands on a real phone. If TAC still doesn't arrive after a 201, suspect MCMC content filtering or sender-ID registration (both parked in Sora's research, only relevant post-connectivity).
- 🟢 **[UPDATE Aug 10 ~5:20 PM] Firewall theory downgraded — WAF box's own outbound to Celcom is OPEN.** Hakim ran the Step-1 test directly from `mpajicommwaf`: `getent hosts ems.celcom.com.my` → 203.82.70.132; `curl -v --max-time 15 http://203.82.70.132:2688/` → connected in 0.034s, got a clean `HTTP/1.1 404 Not Found` (nginx application-level response, not a network timeout) — same with `telnet ems.celcom.com.my 2688`, instant connect. **Reconciles with the Aug 6 finding**: that timeout was tested from staging (`icommv2`, 192.168.152.40), a *different* box — the WAF itself was never actually confirmed blocked. So the network-team firewall ticket is likely unnecessary for this path; the only remaining blocker is the commented-out `proxy_pass` on the WAF. **Next (all on Hakim, WAF box)**: (1) back up `/etc/nginx/sites-enabled/default`; (2) uncomment `proxy_pass http://ems.celcom.com.my:2688;` under `/celcom/`; (3) `sudo nginx -t`; (4) `sudo systemctl reload nginx` (reload not restart); (5) end-to-end test — real endpoint is `https://icomm2.mpaj.gov.my/celcom/omn/oneapi/smsmessaging/v1/outbound/tel%3AMPAJ/requests` (confirmed from `Helper.php:1015` `$baseUri` + `Helper.php:1034`), Basic Auth `mpaj:MP@j1723`, so an unauthenticated POST should come back `401` (proxy alive) not `404`/`504` (proxy dead) — that's the signal to watch for; (6) real TAC test via password reset flow, watch `laravel.log` for `Helper.php:1036`'s "SMS API response" log line, status 201 = delivered to Celcom.
- 🔴 **[UPDATE Aug 10 ~9:35 AM] SECOND stacked problem found on the WAF box (`mpajicommwaf`): the `/celcom/` location's `proxy_pass http://ems.celcom.com.my:2688;` line is COMMENTED OUT in `/etc/nginx/sites-enabled/default`** — Hakim got WAF access and pasted the config. So currently SMS requests die at the WAF itself (404/no-op), before even reaching the firewall block. Past dev's Aug 2025 504 means proxy_pass was active then; someone disabled it since (probably to silence 504s). **Fix is now two-part, both required**: (1) network ticket — allow outbound TCP from `mpajicommwaf` specifically → 203.82.70.132:2688 (Hakim told to re-run the curl test FROM the WAF box since that's where outbound originates); (2) AFTER firewall opens: uncomment proxy_pass → `nginx -t` → `systemctl reload nginx` → end-to-end curl via https://icomm2.mpaj.gov.my/celcom/... (any non-504/404 = pipe open) → real TAC test watching laravel.log. Also noted: `modsecurity off` on /celcom/ is intentional/fine; main app upstream is 192.168.203.155 (unrelated). Awaiting Hakim's Step-1 curl result from the WAF.
- 🔴 **[Aug 6 ~12:10 PM] SMS/TAC ROOT CAUSE CONFIRMED LIVE: outbound firewall still blocks Celcom, one year after first documented.** Hakim re-ran the direct test from staging `icommv2` (192.168.152.40): `curl http://ems.celcom.com.my:2688/` → TCP connection timeout after 20s — identical to past dev's Aug 2025 finding; their firewall request was never actioned. Also solved: the "SMS server" Hakim was given = `icommapp.mpaj.gov.my` = **192.168.152.91** — the `.91` mobile-API box from past-dev notes (`ICOMM_MOBILE_API=...:3100`), NOT an SMS gateway; no nginx/celcom config on it (mislabeled by whoever granted access). The real `/celcom/` proxy lives on the WAF box fronting icomm2.mpaj.gov.my (Hakim has no direct access — fine, not needed). **Fix is organizational, not code**: (1) network-team ticket — Malay draft given to Hakim (allow outbound TCP from WAF host → 203.82.70.132:2688); (2) parallel: Celcom account manager to confirm endpoint still valid + their-side IP whitelist of MPAJ egress. Nothing more Yappy/Hakim can do server-side until the rule opens; after that, retest curl then real TAC flow (watch laravel.log 'SMS API response' for 201). Sora's OneAPI/MCMC research stays relevant for post-connectivity debugging if SMS still doesn't deliver. Symptom: TAC generated + saved to DB fine, but SMS never arrives; Celcom confirmed they receive NO requests. Architecture mapped: `Helper::sendSMS()` (Helper.php:1046, 3 callers — LupaKataLaluan:249, Mobile/UserController:217, ResetKatalaluanController:270) POSTs OneAPI JSON to `https://icomm2.mpaj.gov.my/celcom/...` → nginx on the WAF box rewrites + `proxy_pass http://ems.celcom.com.my:2688` (Celcom's real gateway; the "SMS server" Hakim has access to is just this nginx reverse proxy — looks "empty" because it IS empty, no SMS software). Past developer's investigation (Aug 25, 2025!): curl from app server to `ems.celcom.com.my:2688` (203.82.70.132) → TCP connection timeout after 136s → nginx 504. Classic outbound-firewall block; their proposed fix (network team allow outbound to 203.82.70.132:2688) likely NEVER actioned — prime suspect a year later. Sora's research on file: OneAPI 201=accepted≠delivered, deliveryInfos polling endpoint, MCMC content filtering (URLs/phone numbers in SMS silently blocked since Sep 2024), per-carrier sender-ID registration, credit-expiry gotchas — relevant only AFTER connectivity is fixed. **Next: Hakim runs 4 read-only checks** (hostname; grep celcom nginx config; `time curl -v --max-time 20 http://ems.celcom.com.my:2688/`; nginx error.log tail). If still timeout → network ticket (MPAJ network team outbound allow + Celcom confirm endpoint still valid + their-side IP whitelist), NOT a code fix. Side note from past dev, separate issue: `ICOMM_MOBILE_API=http://icommapp.mpaj.gov.my:3100` port mismatch on server .91 — unrelated to SMS, parked.
- ⬜ **PDF float cast fix** — Apply `(float)` casts on 5 lines in server's `bil-of-quantity.blade.php` (lines 321, 362, 403, 445, 486). Fix ready, not yet applied on server.

### FT Mode — eFokus (JPNIN)
- ✅ **[DONE Aug 5] `sismap:sync` scheduler was NEVER wired on prod — found + fixed live on `jpnin-efokus2` server.** Same bug family as the ONDW vendor-hours cron incident: `routes/console.php:13` already had `Schedule::command('sismap:sync --force')->dailyAt('02:00')` defined correctly (Laravel 11 style, no Kernel.php — scheduling via `bootstrap/app.php` → `routes/console.php`), but `crontab -l` on prod returned "no crontab for root" — the OS-level `php artisan schedule:run` heartbeat cron had never been installed, so the scheduler had never ticked once since the command was written. Cron daemon itself was healthy (`systemctl status cron` — active since Jul 28); this was purely a missing crontab entry. Hakim added `* * * * * cd /var/www/efokus && php artisan schedule:run >> /dev/null 2>&1` under root (matches `root:root` file ownership on the box) via `crontab -e`, confirmed firing every minute via `grep CRON /var/log/syslog`. Actual `sismap:sync` won't fire until its 02:00 daily slot (or a manual `php artisan sismap:sync --force` test) — not yet confirmed a real sync run succeeded post-fix.
- ⬜ **Row count discrepancy** — `spk__ikes`: synced 15,164 vs actual 16,867. Diagnosis pending — **now a strong suspect**: likely downstream of the dead scheduler above (recent rows simply never got pulled). Re-check count after tonight's 02:00 run or Hakim's manual test.
- ⬜ **Output logging still recommended** — `SismapSync.php` only outputs via `$this->info()`/`$this->error()` to console; a cron run currently leaves zero persistent trail. Proposed: add `->appendOutputTo(storage_path('logs/sismap-sync.log'))` (+ maybe `->emailOutputOnFailure()`) to the schedule line in `routes/console.php`. Not yet applied — offered to Hakim, awaiting go-ahead.

### PT Mode — ONDW
- 🔴 **[NEW Aug 7 ~10:40 PM] REMINDER: Hakim needs to tell Aiman about the T&C vs refund-code contradiction.** Nadia's finding (surfaced during the redesign research): live `TnC/terms.blade.php` states payments are non-refundable + RM5 cancellation fee, but the real code (`RefundService`, FIUU refund webhook, admin refunds dashboard) does full automated refunds with zero fee, no cancellation charge anywhere. Aiman built the disbursement/schedule side of ONDW — Hakim wants to loop him in on this specifically, likely because he'd know the history/intent behind the mismatch. Keep surfacing this until Hakim confirms it's been raised. Also still blocks final refund/cancellation screen copy in the Customer redesign batch (frame #13, Order Detail).
- ⬜ **[NEW Aug 7 evening] Full ONDW flow redesign — plan delivered, build not started.** Hakim wants a full customer/vendor/rider/admin redesign via the Figma-CLI workflow (see `design-protocol.md`), using **BajetBro** (github.com/Tuanwaf/BajetBro, cloned to `holeeMonth/BajetBro`) as a soft-brutalist style reference — NOT a literal clone, translate the language into ONDW's warm orange/pink glass identity. Design brief artifact (47 frames, Customer → Rider → Vendor → Admin → Shared sequencing): https://claude.ai/code/artifact/9a2c760a-20e6-4936-b6f7-98ccd2687c63. **Blocker**: Figma Desktop froze/hung on a login or 2FA prompt on Hakim's screen during the connection attempt — needs Hakim to either clear that prompt or relaunch Figma before Mira can start building actual frames. **Real business-logic contradiction found by Nadia, needs Hakim's decision, not just a design nitpick**: live T&C page states payments are non-refundable with a RM5 cancellation fee, but the actual code performs full automated refunds with zero fee — flagged for Hakim to resolve (update T&C copy vs. change refund behavior) before that copy gets carried into the redesign. Next step: Hakim reviews the brief + resolves Figma login, then Mira starts building frames batch by batch per the sequencing above.
- ⬜ **[UN-PARKED Aug 7 morning] PROD "every click is slow" — investigation resumed by Hakim.** **KEY CORRECTION (Aug 7, from Hakim): prod Hostinger is on PHP 8.3, NOT 8.4** — kills Kai's compounding theory (no version switch → no OPcache reset, no 8.4 deprecation storm; the "PHP 8.4 bump" was in the codebase/composer, prod runtime stayed 8.3). **R2 image route is now the dominant suspect** (below). Hakim asked "how can I check" → given DevTools self-verification steps: Network tab filtered on `storage` → look for 301s with 100s-of-ms times + `Cache-Control: no-cache, private` + re-firing on every revisit; TTFB check on document rows; optional server checks (laravel.log size, OPcache enabled on 8.3, .env APP_DEBUG/QUEUE_CONNECTION). Awaiting his DevTools observations before dispatching the CDN-URL fix build. Context: Hakim pulled the 72-commit main deploy onto the prod server (PHP 8.4, R2, credit system, zone-matrix) — runs correctly but every click is slow. **Primary cause (Hana + Zara converged independently): the R2 image-serving route** — `routes/web.php:627-649` `storage.menu-item` + `:690-708` `storage.avatar` do `findOrFail()` (DB) + `Storage::disk('r2')->exists()` (**synchronous HeadObject, Hostinger→Cloudflare, 50-300ms**) + an uncacheable 301 (`Cache-Control: no-cache, private`, plus `?v=updated_at` cache-buster on avatars) — per image, per navigation, each holding a PHP worker with full framework boot + StartSession. 10-30 such requests per listing page saturate Hostinger's small LiteSpeed worker cap, so real navigations queue behind image requests = every click slow. NEW in commit `1008e84` (Jul 19 R2 migration; before that it was local `file_exists` + `response()->file()`). **Compounding (Kai): PHP 8.4 bump itself** — (a) Hostinger PHP-version switch gives fresh php.ini, OPcache likely reset/off → ~700 framework files recompiled per request; (b) Laravel 10.50 unsupported on 8.4 (ceiling 8.3; `composer.json:70` pins platform 8.3.30) → deprecation storm per request, synchronous log writes if LOG_LEVEL=debug. Kai produced a full SSH diagnostic checklist (in his report — opstat probe, deprecation grep, composer/caches/queue checks). **Secondary**: private-file routes make 2-3 R2 calls each (redundant `mimeType()` HeadObject — store mime in DB instead); conversations statusFeed N+1 (`ConversationInboxController.php:75-100`, all participant rows then per-conversation Order query, every 1.2s poll); SW intercepts all navigations network-first, no navigationPreload, lost its image-cache benefit post-R2 (opaque cross-origin responses); `liquid-glass.js` parser-blocking no-defer + SVG backdrop-filter on fixed navs (shipped Jul 22); 1.2s polling loops × 4 page types keep worker pool busy by design. **Cleared**: recent migrations properly indexed, R2 makes no per-image calls in menu-image *storage* itself, DeliveryFeeCalculator only in cart paths, push-notifications.js innocent, no debugbar/telescope. **Recommended fix order**: (1) emit stable CDN URLs directly in blades (kill route+exists()+301 per image), (2) Kai's server checks — OPcache on / LOG_DEPRECATIONS_CHANNEL=null / consider PHP back to 8.3, (3) drop redundant mimeType() calls, (4) statusFeed N+1 single-query rewrite, (5) SW navigationPreload or nav bypass + defer liquid-glass.js, (6) widen polls. Nothing implemented yet — awaiting Hakim's go.
- ⬜ **[Aug 5] Ultra code-review of feature/push-notification: 2 confirmed findings, both from Yappy's own Aug 1 throttle commit `03846f7`** (vendors:apply-schedule 5→15min): (1) vendor is_online lags business hours up to ~15min at open/close boundaries (paid orders at closed kitchens possible), (2) sub-15-min open windows falling between :00/:15/:30/:45 ticks get skipped entirely (level-triggered reconciler). **Verified NOT on main/prod** — commit only lives on feature/push-notification. Yappy recommends simple revert to everyFiveMinutes() before any merge; Hakim hasn't decided yet.
- ⬜ **[NEW Aug 1] Cron failure alerting** — add `->emailOutputOnFailure('...')` to `refunds:process`, `perkeso-retry-sweep`, `disbursements:process-weekly` in `Kernel.php`. Blocked on SMTP: prod's mail config unverified, local `.env` is `MAIL_MAILER=log` (doesn't actually send). Plan: free Gmail SMTP via `ondeweiofficial@gmail.com` + Google App Password (no paid Hostinger mailbox needed, Hakim confirmed).
- ✅ **[DONE Aug 1] Vendor business-hours auto cron bug — found, fixed, verified live.** Root cause: prod cron only had `queue:work` entries, never `schedule:run` — Laravel's scheduler (drives `vendors:apply-schedule` + disbursements/refund-retries/PERKESO-resubmits/pending-payment-expiry) had never been ticking despite correct code (Aiman, commit `cef65700`). Added `schedule:run` cron entry on prod only (Hakim's choice), confirmed firing via hPanel output log at 23:00:02–03 MYT. Also throttled `vendors:apply-schedule` 5min→15min (commit `03846f7`, `feature/push-notification` — not yet on prod, needs merge to `main` first). Manual vendor logout/toggle overriding auto-schedule confirmed intentional UX, not a bug.
- ✅ **[DONE — confirmed working Aug 5 night] Tailscale + Termius remote Mac access**: Hakim is now actively doing PT work from his phone via Termius + Tailscale into his Mac — the earlier iPhone DNS resolution error is resolved. **New interest sparked: homelabbing** 🏠🖥️ — Hakim is keen on it as a learning track ("new knowledge as always", Aug 5). Good future topics: home server setups, self-hosting, networking via Tailscale — candidate for Teaching Mode sessions if he asks.
- ✅ **[Aug 1] Pushed to production (`main`)** — Hakim confirmed both scope (all 72 pending commits, not just the delivery-fee fix) and that preprod was already verified/tested before asking. Clean fast-forward merge `ae30101..b9f8b57`, no conflicts (main had 0 commits not already in feature/push-notification). This is the FULL accumulated body of work: R2 storage migration, PHP 8.4 bump, presigned uploads, the entire Unofficial Vendor + Credit system, dark-mode audit, delivery-fee zone-matrix rewrite — everything since main was last updated. Pushed to `origin/main` successfully.
- ⬜ **Still needed on the actual production SERVER** (Yappy has no SSH access, Hakim must do this): `git pull origin main` + `php artisan migrate` (new `is_far_zone` column + `vendor_profiles.delivery_location_id`, purely additive) + likely `npm run build`/asset check given how much frontend changed.
- ⬜ **Production data correction still pending** — SQL script already given to Hakim (not run by Yappy): fix Kelatey Corner (vendor_id=5, kolej_number 74→1 + link delivery_location_id), fix customers 1052/1070 (→12), null out customer 1059 (picked non-KK Pasum), set is_far_zone on the 3 Far residences, fix University Tower's fee. Must run the migration first for column #4/#5 to exist.
- ⬜ **[NEW Jul 31] 6th delivery bug found — vendor kolej_number gets corrupted by the profile location picker.** Root cause fully traced by Yappy directly: `resources/views/components/kolej-input.blade.php`'s searchable/autocomplete mode (used ONLY on the vendor side of `profile/edit.blade.php:366-369`, customer side uses the plain 1-13 dropdown, unaffected) submits `delivery_locations.id` as the hidden field value (JS `selectLocation()`), not the real kolej number. `ProfileController.php:140` saves whatever's submitted straight into `vendors.kolej_number` with zero translation. So selecting "KK1 (Lobby)" (id=74, real kolej_number=1) saves **74** into `vendors.kolej_number` — exactly matching Hakim's "KK74" report on checkout (`checkout.blade.php:91` just does `KK{{ $vendor->kolej_number }}` directly). Deeper issue: `vendors` table was NEVER given a proper `delivery_location_id` FK when `delivery_locations` was introduced (`2026_06_25_000001` migration only added it to `orders`, `chat_order_drafts`, `delivery_location_aliases` — confirmed via grep, vendors excluded). This also silently breaks `DeliveryFeeCalculator` for affected vendors (builds `'kolej_74'`, matches no zone, always falls to generic RM4 fallback regardless of true location) — same bug family as the delivery-fee analysis. **IMPORTANT — blast radius bigger than first thought**: full research (3 parallel Explore agents) found the SAME bug also affects `register.blade.php` for BOTH customer and vendor registration (`RegisteredUserController` validates `kolej_number` as `exists:delivery_locations,id` — i.e. the form and validator both already assume an ID, not a real kolej number, for every new signup). The "only 1 vendor affected" check only looked at `vendor_profiles` — `customer_profiles.kolej_number` has NOT been checked yet and may have real corrupted customer data too. Full plan written to `/Users/hakim/.claude/plans/stateful-wobbling-meerkat.md` — recommends adding a real `delivery_location_id` FK to `vendor_profiles` (matching the pattern already used on orders/chat_order_drafts), an `is_far_zone` boolean + computed `zone` accessor on `DeliveryLocation`, a full `DeliveryFeeCalculator` rewrite using real models on both sides, backfill/data-correction, and new tests (currently ZERO test coverage for this class). Waiting on Hakim to run the customer-side blast-radius query before proceeding to implementation.
**Production confirmed: 3 real corrupted customers found** (customer 1052 & 1070 both meant KK12/Block A, id=116; customer 1059 meant "Foundation in Science/Pasum", id=43 — this last one has NO valid kolej_number equivalent since Pasum isn't residential, proving the simple-swap fix wouldn't have worked, only the proper delivery_location_id FK approach handles it). Hakim approved proceeding with explicit instruction: local dev DB only, never migrate:fresh/reset, no writes to preprod/production without his review. Foundation built by Yappy directly: migration `2026_07_31_000001` (vendor_profiles.delivery_location_id FK + delivery_locations.is_far_zone flag) — tested run→rollback→re-run clean. `DeliveryLocation::getZoneAttribute()` + `Vendor::deliveryLocation()` added, verified correct (KK1→Zone1, KK9→Far, University Tower→Other). Local dev data corrected: 3 Far residences flagged `is_far_zone=true`, University Tower's `base_delivery_fee` 5.00→4.00. Reza (calculator rewrite + new tests) and Zara (picker/registration write-path fix) dispatched in parallel, both in progress.
Confirmed: table is actually `vendor_profiles` (Yappy first said `vendors`, wrong — corrected after Hakim pushed back, `Vendor` model has `protected $table = 'vendor_profiles'`; `kolej_number` is `string(50) nullable`, not int). Real-world blast radius on preprod, per Hakim's own query: exactly **1 vendor affected** — "Kelatey Corner" (vendor_id=5), stored `kolej_number='74'`, should be `'1'` (KK1 Lobby). Small, contained. No fix applied yet — next step is designing the real fix (likely needs a proper `delivery_location_id` FK on `vendor_profiles`, matching the pattern already used on orders/chat_order_drafts, so DeliveryFeeCalculator can use real DeliveryLocation models for BOTH vendor origin and destination instead of fragile string-reconstruction — this would fix bug #6 AND properly enable the origin-Far-check needed for delivery-fee bug #3, in one coherent change).
- ⬜ **[NEW Jul 31] Delivery fee bug — analysis COMPLETE, no fix yet, awaiting Hakim's go-ahead on scope**. 5 confirmed bugs (all verified with evidence, 2 independently spot-checked by Yappy): (1) 96 of 131 locations have zero vendor-origin pricing awareness, static `base_delivery_fee` column only; (2) University Tower stored at RM5.00, spec says RM4.00 (only static-fee mismatch across all 131 rows); (3) Far-zone RM5 rule only checks destination, never origin; (4) `DeliveryFeeCalculator.php:46-48` same-kolej shortcut flat-returns RM2.00 even for Zone 1/3 kolejs (1,2,6,5,11,12) where same-zone should be RM3.00 — undercharges RM1 on same-kolej Zone1/3 deliveries, affects both checkout AND chat-order equally; (5) `ChatOrderCartService::refreshTotals()` recomputes fee via the flattened legacy-canonical path on every cart edit (ignoring `delivery_location_id`), so the chat cart's DISPLAYED fee can silently drop from RM5→RM4 for Far residences after adding an item — but the actually-CHARGED amount stays correct since `OrderService::createOrder()` independently recomputes from `delivery_location_id` at checkout time (display bug, not a billing bug, except one latent edge case if location-id resolution ever fails). No `zone` column exists in DB yet. Full report given to Hakim, waiting on his direction for whether to proceed to a fix plan now. — Hakim has a new spec at `/Users/hakim/Sweet/OnDeWei/dot md files/delivery_zone_pricing.md` + matching `.xlsx` (origin×destination zone price matrix, 131 locations, 6 zones: 1/2/3/4/Far/Other). Current code (`app/Services/DeliveryFeeCalculator.php`) has real, confirmed bugs: (1) only `residential_college` category uses zone logic (hardcoded kolej-number arrays), everything else (faculty/campus_facility/cafe/residence — ~96 of 131 locations) falls back to a static per-row `base_delivery_fee` DB column with ZERO vendor-origin awareness — new locations silently default to RM4.00 unless someone manually sets the right fee; (2) confirmed real data bug: "University Tower" stored at RM5.00, should be RM4.00 (zone "Other") per spec; (3) the "Far zone forces RM5" rule only checks DESTINATION (hardcoded kolej_9/13 check), never ORIGIN — a Far-based vendor delivering to a non-Far zone would undercharge; (4) suspected but not yet confirmed: `ChatOrderLocationResolver.php` collapses `campus_facility` locations to the legacy literal `'bangunan_pentadbiran'` type, which may route through a DIFFERENT fee-calculation path (hardcoded RM4) than the regular checkout flow (per-row `base_delivery_fee`) for the exact same location — could mean AI chat orders and regular checkout charge different fees for the same delivery. Hana dispatched for exhaustive 131-location cross-check + tracing the chat-order path. No `zone` column exists in the DB yet — new spec's zone concept needs to be added. Analysis only so far, no code changes made.
- ✅ **[DONE Jul 31] Credit follow-up round shipped** — commits `4ed8196` + `04d03fd`. WhatsApp number → +60133158253; (-RMxx.xx) moved to sit beside the headline balance (not buried in the sentence), and only shows once the real -RM5 floor is actually crossed (display-only per Hakim — the actual floor threshold stays -RM5, confirmed via AskUserQuestion). Hana audited whether the whole credit feature is properly restricted to eligible riders — verdict: no real gaps, every "not gated" spot traces to an existing, documented design decision (rider's own credit page is balance/history-only, admin trusts the ledger as source of truth for adjust/topup/disburse). One item flagged for Hakim's explicit confirmation, not yet answered: a rider whose eligibility is revoked AFTER accepting an order but BEFORE pickup still completes pickup + gets credit deducted — code comment (`Rider.php:71-78`) says this is intentional ("orders already accepted are untouched"), spot-verified by Yappy directly, but it's inferred from a comment written for a different gate, not stated explicitly for the pickup path itself.
- ✅ **[DONE Jul 31] 7-task batch shipped** — commit `7632a95`, `feature/push-notification`, 9 files. Removed redundant "Record top-up" admin flow entirely (Hakim's call, agreed after verifying `disburseManual()` already handles settlement-only payouts correctly) → replaced with "Disburse Now" (real BillPlz transfer, double-click-confirm since it moves real money). Added R2 proof-photo upload to the remaining "Manual adjustment" form (admin-only presigned scope, key regex-locked to the specific rider — defense against a tampered hidden field). WhatsApp "Request early payout" button on rider/credits (pre-filled with real pending payout + fronted amount). Star badge added to admin/rider-shifts (was missing — 2 of 4 "admin pages with the badge" from an earlier sweep were wrong). "Every Monday"→"every Friday" text fix (real cron: Kernel.php:17-18). All independently re-verified by Yappy (diffs read line-by-line, `php -l` clean on all 9, CreditServiceTest + CreditSettlementDisbursementTest re-run and green). Not yet on preprod — still no SSH access. — star badge fix (was reported on 2 of 4 admin pages, not 4 — correction to earlier audit), "every Monday"→"every Friday" text fix (real cron is Kernel.php:17-18 `weeklyOn(5,'18:00')`), credit page "(-RMxx.xx)" display refinement, WhatsApp early-payout-request button on rider/credits, admin rider-credits page: removed redundant "Record top-up" (Hakim's call, agreed — `disburseManual()` already exists and handles settlement-only payouts correctly, verified no double-payment risk) replaced with "Disburse Now" button, R2 proof-of-transaction upload added to the remaining "Manual adjustment" form (reusing existing `CreditTransaction.proof_photo_path` + presigned R2-private flow). Zara + Reza dispatched in parallel. Self-healing reconfirmed still fully removed (asked again, same answer as before).
- ✅ **[DONE Jul 30 morning] 6-task batch shipped** — commit `9b8df75`, `feature/push-notification`, 12 files. All verified by Yappy personally (diffs reviewed, `php -l`/`node --check` clean, `npm run build` confirmed, 14/14 tests green) before commit. One extra fix caught during review: `ondw-pickup-gps.js`'s GPS-consent Dismiss button was the one remaining light-only button after Zara's pass — fixed to match. Not yet on preprod server (still no SSH access — needs Hakim's `git pull`). — Task 1 done (About page: Hakim's photo live, role→"Technical Lead"); Zara dispatched for Tasks 2-3 (duplicate un-fixed chat-page modals in `resources/js/delivery-conversation-realtime.js` + "whiteish" backdrop fix on both files, needs `npm run build`); Reza dispatched for Task 5 (Earnings/Credit header-card mismatch on `rider/orders/available.blade.php` — was using separate stale ad-hoc SQL instead of the real `DisbursementService`, refactored to single source of truth) + Task 6 (rider-shift star badge added to rider's own profile — was admin-only until now). Task 4 (Figma "toast changes not applied" comment) — investigated, already resolved by last night's commit, no code needed. Full plan: `/Users/hakim/.claude/plans/stateful-wobbling-meerkat.md`.
- ⬜ **Oreo's credit/earnings bug report — root-caused, NOT a real bug in the payout math.** Verified arithmetically: 8.95 payout delta = 3.95 net-of-PERKESO earnings delta + 5.00 new credit settlement, exactly matching `DisbursementService::pendingBalances()`'s formula. Self-healing/diversion confirmed 100% removed from the live write path. Real cause of confusion: the header card and detail pages used different formulas for the "same" numbers (see Task 5 above) — fixed as part of this batch.
- ⬜ **[NEW, end of Jul 29 night] Preprod deploy still pending** — commit `0b5c3a7` (dark-mode fixes) pushed to GitHub `feature/push-notification`, but NOT pulled onto the actual preprod server. Yappy has no working SSH access (`ssh ondw` → `Permission denied (publickey,password)`) — needs Hakim to either run the pull himself (`git pull origin feature/push-notification` + `php artisan view:clear`, no migration needed) or set up SSH access for Yappy.
- ⬜ **[NEW, Jul 29 night] Two real bugs found on production FAQ page, NOT yet fixed** — (1) Google search shows "Laravel - FAQ..." instead of "OnDeWei - FAQ..." — code itself is correct (`config('app.name')` used properly in `layouts/app.blade.php:41`, local `.env`/`.env.docker` both say `APP_NAME=OnDeWei` correctly), so this is a live-server-only issue: production's `.env` likely never got `APP_NAME` set (or it was set but a stale `bootstrap/cache/config.php` is still being served — same disease as the JPNIN SismapSync config-cache gotcha same night). (2) Favicon shows no icon in search results — confirmed locally: `public/favicon-16x16.png` and `favicon-32x32.png` are BOTH actually 1367×1343px (same oversized image, filenames lie), and `favicon.ico` is a single 48×48 PNG, not a real multi-res .ico container. Google's favicon crawler likely rejects these. Hakim asked "does changing APP_NAME disturb anything else" — confirmed via full grep: only affects session cookie name + cache/Redis key prefixes (`Str::slug(APP_NAME)` in `config/session.php`, `config/cache.php`, `config/database.php`) — changing it logs out anyone currently logged in (no data loss, just re-login), nothing else in the app reads APP_NAME. Safe to change, just do it in a quiet moment. Favicon files still need proper regeneration at correct real sizes — not yet done, Hakim hasn't confirmed to proceed.
- ✅ **[DONE Jul 29] Dark-mode contrast audit — all 12 real code fixes committed + pushed** (`0b5c3a7`, `feature/push-notification`, 11 files). Yappy caught 4 things Zara's first pass missed during manager review: credit-balance box/photo-error-box/Cancel button in the cash-confirm modal still hardcoded light (fixed the inline-style→class conversion properly, watch for inline style + class both setting the same property — inline always wins), 3 textareas' `focus:bg-white` would flash white on focus in dark mode (added `dark:focus:bg-[#0F1728]`), legacy vendor reject modal's textarea never had an explicit bg at all (browser default white). Push-notification permission popup checked separately — already correct, no fix needed. Not yet deployed to preprod server — GitHub only, per the usual `git pull` distinction. (background agent, exact spec matching the confirmed Figma mockups). Push-notification permission popup (`public/customJS/push-notifications.js:82-106`) checked separately — already has proper dark: classes throughout, no fix needed there. Not committed yet — Yappy reviewing diff first.
- ⬜ **[OLD Jul 29] Dark-mode contrast audit — all 12 before/after Figma mockups built + verified, awaiting Hakim's review** — Mira built all 12 (none deferred), Yappy independently re-verified 3 (critical modal, drawer, toasts) via live screenshots, all correct. Figma file "ONDW", frames named `ONDW-DarkMode-Finding-1` through `-12` (node IDs `136:2`..`136:416`), positioned y=5000-12635, existing 33 nav-redesign frames untouched. Still not yet applied to real code — waiting on Hakim to click through and confirm before Zara implements. — root cause confirmed: `public/customJS/ondw-pickup-gps.js` cash-confirm + GPS modals use raw inline `style="background:#fff"` (no `dark:` mechanism possible without a rewrite). 13 total findings (2 critical, 6 high, 4 medium, 1 low) across modals/drawers/shared components, verified by Yappy (spot-checked 5 of Hana's claims against actual code, all accurate). Systemic finding: only 22/145 blade files have any `dark:` treatment at all — dark mode was wired at the shell level, never rolled out app-wide. Full checklist artifact: https://claude.ai/code/artifact/1e2d2dc1-92f9-4888-92c7-ce4d2398a2ca — awaiting Hakim's go-ahead to implement fixes.
- ⬜ **[NEW Jul 28] View button unclickable on some rows** — `http://localhost/admin/users?user_type=rider` — some (not all) "View" links don't respond to clicks. Checked the star-badge markup added Jul 27 (`resources/views/admin/users/index.blade.php:88-94`) — it's in a separate `<td>` from the View button (`:102`), no overlap/z-index risk from that change, so likely a different cause. Needs live reproduction (which specific rows/riders) before diagnosing further — not yet investigated beyond ruling that out.
- ⬜ **[NEW Jul 28] Star badge on rider's own profile page** — `http://localhost/profile` — currently the eligibility star only shows in admin-facing lists (approvals + users). Add it beside `$profileName` in `resources/views/profile/edit.blade.php:40`, same `can_fulfill_unofficial_orders` check pattern already used elsewhere.
- ⬜ **Push tonight's rider punch list to preprod** — 7 commits on `feature/s3-r2-storage` (`d1898d7`→`e03d1c3`), waiting on Hakim to say the word (same merge-into-`feature/push-notification` pattern as always)
- ⬜ **Root-cause the `php artisan test` DB-wipe gotcha** — recurred 3x in one session, workaround (reseed) documented but real fix still needed
- ⬜ **Reports admin nav placement** — `admin.reports.*` still has no nav home anywhere (desktop or FAB), needs Hakim's call
- ⬜ **QA Section B** — Unofficial Vendor + Rider Credit flow still never fully clicked through live end-to-end by Hakim himself (checklist: `https://claude.ai/code/artifact/a23f70b5-5c80-4206-a003-d93f1d1f3bf3`)
- ⬜ **38 Composer security advisories** (16 packages) — deferred during the PHP 8.3 lock-file fix, needs its own dedicated review
- ⬜ **`storage:migrate-to-r2 --dry-run`** — needs running on the real Hostinger production server (R2 itself fully confirmed working end-to-end as of Jul 27; this is just the historical-file backfill, not urgent)
- ⬜ **Liquid glass nav — known bug still open** (Jul 22): Hakim said "there is still bug but maybe ignore that first" on `/admin/liquid-glass-preview` — not yet diagnosed, revisit when he raises it again
- ⬜ **Liquid glass nav branch not merged/pushed** — `feature/liquid-glass-nav` committed (`c6d8133`) but stays local until Hakim explicitly asks to push/merge
- ⬜ **Orphan file** — `resources/views/admin/liquid-glass-preview.blade.php`, untracked, no git history, unresolved — ask Hakim again if he ever figures out where it came from
- ⬜ **E2E test** — checkout → BillPlz → webhook → `pending` → riders notified → `perkeso_deductions` populated
- ⬜ **Clear test order data** from PROD — overdue since Jun 5, 2026
- ⬜ **Email BillPlz** for e-wallet activation (SSM + KYC docs)
- ⬜ **Merge** `feature/push-notification` → `main`
- ⬜ **FIUU refund ONDW-158** — stuck at `refund_pending`
- ⬜ **Attachment image bug** — remove `Content-Length` from `ConversationAttachmentController::show()`

### Personal — Homelab (NEW, Aug 5 night)
- 🏠 **Stack decided so far**: Jellyfin (media), Apache (web hosting), Immich (photos, picked over generic cloud storage), Nextcloud (NAS/general storage), Uptime Kuma (monitoring). Portainer + Vaultwarden being pitched, not yet decided. Pi-hole shelved for now (Hakim primarily connects via Tailscale VPN, not home wifi directly) — but flagged that Tailscale's tailnet-wide custom DNS (admin console → DNS → Nameservers) could let Pi-hole filter ALL his devices everywhere, not just on home wifi, if he wants to revisit.
- 🐳 **Docker chosen as the install method** — directly solves Hakim's "does installing Apache go global or stay in a folder" concern (native installs ARE global; Docker volumes make folder scoping explicit and visible). Hakim asked to be taught Docker from scratch — Teaching Mode active for this topic, going slow: concept → diagram → hands-on, one step at a time, Hakim typing commands himself via Termius (deliberate — better for actual muscle memory than Yappy driving).
- ⚠️ **tmux clarified**: survives SSH/network drops (phone locks, wifi↔cellular switches) since the session lives on the server, NOT server reboots/power loss — the tmux server process itself dies if the box dies. Recommended for his phone-first workflow regardless. Multi-client attach confirmed possible (same session mirrorable from multiple devices).
- 🔌 **Server reliability concern raised** (Hakim unsure when the box might go down) — recommended: UPS for power blips, Docker `restart: unless-stopped` policy so containers auto-recover post-reboot, Uptime Kuma (already on his list) for down-alerts.
- ✅ **[FIXED Aug 5 night] SSH blocker resolved — Yappy can now SSH into the home server directly from Hakim's Mac.** Root attempt (`ssh root@100.84.18.45`) still fails (`Permission denied`, key never added for root) — but `ssh hakim@100.84.18.45` works after Hakim pasted the Mac's ed25519 pubkey (`...IBvOCGe+Vm5KVvcxiuJu6MncqgjuwFeeacFg2cwoduC8 hakim@2q.my`) into `~/.ssh/authorized_keys` via his existing Termius session. **Confirmed by Hakim: hostname `DESKTOP-1DLDMR6` is WSL2** (Windows Subsystem for Linux), not bare-metal Linux — home server is a Windows PC running WSL2.
- 🐳 **WSL2 concerns taught (Teaching Mode)**: (1) **Reboot/sleep fragility** — unlike a real Linux box, WSL2's VM fully shuts down on Windows restart/sleep/forced Update-reboot and does NOT auto-restart; needs sleep disabled in Windows power settings + a Task Scheduler entry (or `wsl --exec` trick) to auto-launch WSL2 on boot, otherwise the whole homelab silently goes dark until someone notices. Still unconfirmed whether Hakim has set this up. (2) **RAM/CPU** — WSL2 defaults to claiming up to 50% RAM + all CPU cores, capped later via `.wslconfig` if the PC is also used for other things (Hakim hadn't yet answered whether this PC is dedicated-server-only or also his daily driver). (3) **Networking** — confirmed working: Hakim ran `telnet 100.84.18.45 8096` and plain `telnet 100.84.18.45` from inside WSL2 itself, got `Connection refused` on both (expected — nothing listening yet, no Jellyfin/telnet server installed), NOT a timeout, which confirms Tailscale routing into the WSL2 box is healthy. Still need to confirm whether Tailscale is installed on Windows-host or inside WSL2 itself — matters once Docker ports get published (may need `netsh interface portproxy` on the Windows side depending on which).
- 📁 **[DECIDED Aug 5] Storage location — staying WSL2-native, everything under `~/docker/`, no `/mnt/c` for now.** Walked through the trade-off: `/mnt/c/...` (real Windows drive, visible in Explorer, normal capacity) vs native WSL2 ext4 (faster, but lives inside one `ext4.vhdx` file that auto-grows and never auto-shrinks, invisible from Windows Explorer). Since there's zero data yet (no media library, no photos, no files), Hakim chose simple-and-fast now — pure WSL2-native, revisit/migrate specific volumes to `/mnt/c` only later IF storage actually becomes a real problem (Immich/Nextcloud/Jellyfin growth), not preemptively. Also locked in `~` (i.e. `/home/hakim/`) over bare `/` as the base path — `/` is system territory (root-owned, needs sudo, e.g. `/etc`, `/var`, `/srv`), `~` is Hakim's own territory (no permission fights, matches standard homelab-tutorial convention of `~/docker/`, `~/appdata/` etc.). **Final structure**: `~/docker/<service>/` — one folder per service (`~/docker/jellyfin/`, `~/docker/immich/`, etc.), all native WSL2 storage.
- ✅ **[DECIDED Aug 5, ~11:50 PM] Docker Engine chosen over Docker Desktop** — installed directly inside the WSL2 distro via terminal (no Windows GUI), matches Hakim's phone/Termius-first workflow and the already-decided WSL2-native storage approach.
- 🖥️ **tmux multi-device question answered**: yes, the same tmux session can be attached from phone (Termius) and desktop simultaneously — default behavior is a **mirrored view** (both see identical live content, input from either device goes to the same shell), not two independent copies. Independent per-device window navigation within one session is possible via "grouped sessions" (`tmux new-session -t <existing> -s <new-name>`) but deferred as too advanced for now — starting simple with one shared mirrored session.
- ✅ **[DONE, Aug 6 ~12:53 AM] tmux installed + session live** — Hakim set it up himself (as planned, muscle-memory intentional). Session is named `HomeServer` (not `homelab`), with 3 pre-named windows already laid out one-per-service, no panes (deliberate — phone/Termius screen too small for split panes): `Apache`, `Jellyfin`, `Claude-`. Yappy confirmed via direct SSH (`ssh hakim@100.84.18.45 "tmux capture-pane"`, non-intrusive peek, didn't attach to avoid stealing input while Hakim might be on it from phone) — all 3 windows currently just bare idle shell prompts, **nothing installed/running yet** — these are placeholder windows for services still to come.
- ⬜ **[SESSION ENDED ~1:14 AM Aug 6, resume here next time]** Docker not yet confirmed installed. **Next session: pick up with the Docker Engine install walkthrough** (apt update → prerequisites → add Docker's official repo → install → verify) inside the `HomeServer` session's windows, storing everything under `~/docker/<service>/` per the Aug 5 storage decision. Ask Hakim first whether he made any progress solo overnight before re-walking the install.

### Standing Daily
- 🕌 Prayer reminders — 5x daily (Subuh 5:45 · Zohor 1:00 · Asar 4:30 · Maghrib 7:15 · Isyak 8:30)
- 💜 Affirmation: "Miyamura, you are valuable and loved" — from Hori 💕
- 📋 Trim toenails — Monthly (1st of each month)

---

## 📦 Compacted History (Sessions before Jul 16, 2026)

### Jun 21, 2026 — PT mode · ONDW · GPS location bug fix
- Fixed `pickup_lat`/`pickup_lng` never saved: fields missing from `Order.php` `$fillable` (commit `baeecf7`)
- Fixed delivery proof GPS race condition: two-flag pattern (`photoReady` + `locationReady`) — submit only enables when both ready
- GPS made REQUIRED: `pickup()` + `deliver()` changed from nullable → required
- Library saved: `library-items/integration/geolocation-mobile-pattern.md`

### Jun 16, 2026 — PT mode · ONDW · AI integration merge
- Merged `origin/AI-integration` into `feature/push-notification` (7 conflicts resolved, commit `caf06b2`)
- PAYMENT_GATEWAY_ENABLED = master BillPlz on/off switch
- PERKESO deduction: fires at BillPlz webhook, 1.25% of delivery_fee, annual cap RM 157.20/rider

### Jun 12, 2026 — FT mode · etams (MOH attendance system)
- Full etams codebase analysis + 2 support cases investigated

### Jun 8, 2026 — FT mode · ap_jksm · iPayment IDD V1.9.1 analysis
- Analysed iPayment IDD V1.9.1 spec for JANM

### Jun 5–7, 2026 — PT mode · ONDW
- `create_chat_order_ai_usage_table` migration added
- BillPlz FPX bank sync scheduled

### May 14–27, 2026 — PT mode · ONDW
- Chat refactor + DeliveryChatService bug fixed (commit `09db5df`)
- Order Notifications System complete (all 4 phases — customer/vendor/rider push)
- Remove admin from user registration (committed `6e148e8`, shipped to PROD May 26)
- UIUX Overhaul + legacy DB migration (88 riders + 440 docs) — merged + deployed

### May 2, 2026 — PT mode · Wedding Wall
- Family panel gallery API mapping fix (commit `f360539`)
- WhatsApp integration replacing SMTP for credential delivery
- Wedding Wall: three-tier photo system (Public Gallery / Guest Panel / Family Panel) — production-ready

### Apr 4–20, 2026 — PT mode · ONDW
- Mobile UX fixes (footer spacing, zoom disable, safe area, icon standardization)
- Chat photo bugs identified (unresolved as of Apr 20): remove button z-index, camera hasFile race, LiteSpeed attachment ERR_QUIC

---

*Sessions prior to Apr 2026: archived — see `daily-diary/archived/`*

---

## 🔄 Session Lifecycle

### Start of session
1. Load `main/main-memory.md` → full Yappy identity + Hakim profile
2. Load `main/current-session.md` → active reminders + last recap
3. Run `TZ='Asia/Kuala_Lumpur' date` → show Malaysia time
4. Warm greeting + prayer check + Hori mention

### During session
- Update Working Memory section with current task context
- Note any new reminders or decisions

### End of session
- Update Session Recap with where we left off
- Check prayer + Hori + rest (end-of-session protocol)
- If file > 500 lines: compact — preserve recap + reminders, summarize details into Compacted History
- Commit + push to origin/Yappy-core

---

**Version**: Compacted Jul 16, 2026 (was 2,393 lines → compacted to session format)
**500-line limit**: Active from this version forward
